package com.hbm_m.particle.nt;

/**
 * Маркер для NT-частиц, спавнимых сетевым пакетом (AuxParticlePacket).
 *
 * При повторном получении пакета того же эффекта рядом (перемотка назад
 * и повторный прогон в рекордере вроде Flashback, либо дубликат от сервера)
 * старая частица ЗАМЕНЯЕТСЯ новой вместо наложения — иначе в реплее
 * поверх живого гриба отыгрывается второй (ParticleEngineNT.add).
 *
 * НЕ вешать на непрерывно спавнимые клиентом частицы (контрейлы ракет,
 * дым): соседние экземпляры там — норма, а не дубликат.
 */
public interface ReplaceOnRespawn {
}
